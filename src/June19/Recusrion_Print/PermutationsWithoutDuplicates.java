package June19.Recusrion_Print;

public class PermutationsWithoutDuplicates {

    public static void main(String[] urgi) {

        getPerms("abca", "");

    }

    private static void getPerms(String question, String answer) {

        boolean[] storage = new boolean[256];

        char ch0 = question.charAt(0);

        // Only if call hasn't already been made for this character
        if (!storage[ch0]){

        }

    }

}
